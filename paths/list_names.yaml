post:
  description: Create a new <<glossary:list>> for your organization.
  operationId: create-a-name-list
  parameters:
    - $ref: ../components/parameters/keyInflectionHeader.yaml
    - $ref: ../components/parameters/idempotencyKeyHeader.yaml
    - $ref: ../components/parameters/personaVersionHeader.yaml
  requestBody:
    content:
      application/json:
        schema:
          properties:
            data:
              properties:
                attributes:
                  properties:
                    allow-fuzzy-name-first:
                      description: >-
                        Flag to determine if the list should use fuzzy matching
                        for first name
                      type: boolean
                    name:
                      description: Name of the list
                      type: string
                  type: object
              type: object
          type: object
  responses:
    '201':
      content:
        application/json:
          examples:
            Result:
              summary: Created
              value: {}
          schema:
            properties: {}
            type: object
      description: Created
    '400':
      $ref: ../components/responses/BadRequest.yaml
  summary: Create a Name List
  tags:
    - Lists
