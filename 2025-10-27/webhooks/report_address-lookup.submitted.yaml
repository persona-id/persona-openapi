post:
  operationId: webhook-report-address-lookup-submitted
  summary: Address lookup report submitted
  description: >-
    Webhook for the `report/address-lookup.submitted` event. For more info see
    [Webhooks Overview](https://docs.withpersona.com/webhooks).


    Sent when an address lookup report has been submitted to its upstream data
    source and is awaiting results (`status` is `submitted`). A `ready` or
    `errored` event follows once processing completes.


    The payload is the polymorphic report object. Inspect `payload.data.type`
    (`report/address-lookup`) to identify the report, and
    `payload.data.attributes` for its inputs and results.
  tags:
    - Report events
  x-persona-product: Reports
  parameters:
    - $ref: ../components/parameters/persona-signature-header.yaml
    - $ref: ../components/parameters/persona-webhook-attempts-made.yaml
    - $ref: ../components/parameters/persona-webhook-attempts-left.yaml
    - $ref: ../components/parameters/persona-webhook-first-attempted-at.yaml
  requestBody:
    content:
      application/json:
        schema:
          type: object
          additionalProperties: false
          properties:
            data:
              type: object
              unevaluatedProperties: false
              allOf:
                - $ref: ../components/schemas/webhook-body-shared-data.yaml
                - type: object
                  properties:
                    attributes:
                      unevaluatedProperties: false
                      allOf:
                        - type: object
                          properties:
                            name:
                              type: string
                              const: report/address-lookup.submitted
                            payload:
                              type: object
                              additionalProperties: false
                              properties:
                                data:
                                  $ref: ../components/schemas/report.yaml
                                included:
                                  description: >-
                                    Objects referenced by
                                    `payload.data.relationships` are included
                                    here when related-object inclusion is
                                    enabled for the webhook. When inclusion is
                                    off (the default), the `included` key is
                                    omitted entirely (it is not returned as an
                                    empty array).
                                  $ref: >-
                                    ../components/schemas/reports-included-objects.yaml
                        - $ref: >-
                            ../components/schemas/webhook-body-shared-attributes.yaml
        examples:
          Example Body:
            $ref: >-
              ../components/examples/report-address-lookup-submitted-webhook-body.yaml
  responses:
    '200':
      $ref: ../components/responses/success.yaml
