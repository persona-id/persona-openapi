post:
  operationId: webhook-transaction-sentinel-session-processed
  summary: Transaction sentinel session processed
  description: >-
    Webhook for the `transaction.sentinel-session-processed` event. Fires when a
    Sentinel session finishes processing for a Transaction. For more info see
    [Webhooks Overview](https://docs.withpersona.com/webhooks).
  tags:
    - Transaction events
  x-persona-product: Transactions
  parameters:
    - $ref: ../components/parameters/persona-signature-header.yaml
    - $ref: ../components/parameters/persona-webhook-attempts-made.yaml
    - $ref: ../components/parameters/persona-webhook-attempts-left.yaml
    - $ref: ../components/parameters/persona-webhook-first-attempted-at.yaml
  requestBody:
    content:
      application/json:
        schema:
          type: object
          additionalProperties: false
          properties:
            data:
              type: object
              unevaluatedProperties: false
              allOf:
                - $ref: ../components/schemas/webhook-body-shared-data.yaml
                - type: object
                  properties:
                    attributes:
                      unevaluatedProperties: false
                      allOf:
                        - type: object
                          properties:
                            name:
                              type: string
                              const: transaction.sentinel-session-processed
                            payload:
                              type: object
                              additionalProperties: false
                              properties:
                                data:
                                  $ref: ../components/schemas/transaction.yaml
                                included:
                                  description: >-
                                    All objects that are present in
                                    `data.attributes.relationships` will be
                                    provided in `included`.
                                  $ref: >-
                                    ../components/schemas/transactions-included-objects.yaml
                        - $ref: >-
                            ../components/schemas/webhook-body-shared-attributes.yaml
        examples:
          Example Body:
            $ref: >-
              ../components/examples/transaction-sentinel-session-processed-webhook-body.yaml
  responses:
    '200':
      $ref: ../components/responses/success.yaml
