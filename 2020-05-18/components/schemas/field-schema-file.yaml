type: object
additionalProperties: false
properties:
  type:
    type: string
    const: file
  key:
    type: string
  label:
    type:
      - string
      - 'null'
  config:
    unevaluatedProperties: false
    allOf:
      - $ref: ./field-schema-default-config.yaml
      - type: object
        properties:
          max-file-size-bytes:
            type: integer
            default: 10000000
          min-file-size-bytes:
            type: integer
            default: 1
          supported-mime-types:
            type: array
            items:
              type: string
            default:
              - >-
                image/jpg image/jpeg image/png image/heic image/heif image/tif
                image/tiff image/gif image/svg+xml application/pdf text/plain
                text/csv video/mp4 video/webm video/quicktime
