description: >-
  The number of requests remaining in the current rate limit window for the API
  key used to authenticate the request. Returned on every authenticated
  response.
schema:
  type: integer
  examples:
    - 299
