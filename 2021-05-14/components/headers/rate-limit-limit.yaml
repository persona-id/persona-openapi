description: >-
  The maximum number of requests permitted in the current rate limit window for
  the API key used to authenticate the request. Returned on every authenticated
  response.
schema:
  type: integer
  examples:
    - 300
