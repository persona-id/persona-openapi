post:
  operationId: webhook-account-updated-tags
  summary: Account updated tags
  description: >-
    Webhook for the `account.updated-tags` event. The `context` identifies the
    tags added and removed by the update. For more info see [Webhooks
    Overview](https://docs.withpersona.com/webhooks).
  tags:
    - Account events
  x-persona-product: Accounts
  parameters:
    - $ref: ../components/parameters/persona-signature-header.yaml
    - $ref: ../components/parameters/persona-webhook-attempts-made.yaml
    - $ref: ../components/parameters/persona-webhook-attempts-left.yaml
    - $ref: ../components/parameters/persona-webhook-first-attempted-at.yaml
  requestBody:
    content:
      application/json:
        schema:
          type: object
          additionalProperties: false
          properties:
            data:
              type: object
              unevaluatedProperties: false
              allOf:
                - $ref: ../components/schemas/webhook-body-shared-data.yaml
                - type: object
                  properties:
                    attributes:
                      unevaluatedProperties: false
                      allOf:
                        - type: object
                          properties:
                            name:
                              type: string
                              const: account.updated-tags
                            context:
                              type: object
                              additionalProperties: false
                              description: >-
                                Identifies the tag names added to and removed
                                from the Account.
                              properties:
                                added-tags:
                                  type: array
                                  description: >-
                                    Names of tags added by this update. Empty if
                                    none were added.
                                  items:
                                    type: string
                                  examples:
                                    - - manual-review
                                removed-tags:
                                  type: array
                                  description: >-
                                    Names of tags removed by this update. Empty
                                    if none were removed.
                                  items:
                                    type: string
                                  examples:
                                    - - new-customer
                            payload:
                              type: object
                              additionalProperties: false
                              properties:
                                data:
                                  $ref: ../components/schemas/account.yaml
                                included:
                                  description: >-
                                    All objects that are present in
                                    `data.attributes.relationships` will be
                                    provided in `included`.
                                  $ref: >-
                                    ../components/schemas/accounts-included-objects.yaml
                        - $ref: >-
                            ../components/schemas/webhook-body-shared-attributes.yaml
        examples:
          Example Body:
            $ref: ../components/examples/account-updated-tags-webhook-body.yaml
  responses:
    '200':
      $ref: ../components/responses/success.yaml
