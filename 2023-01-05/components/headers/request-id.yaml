description: >-
  The unique identifier of the API log entry that recorded this request. Starts
  with `req_`. Include this value when reaching out to Persona support. Present
  on every authenticated response, including error responses.
schema:
  type: string
  examples:
    - req_AbcDeFGhIjKlMnOpQrStUvWxYz1
