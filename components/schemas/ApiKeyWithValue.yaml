type: object
title: API Key
description: >-
  An Api Key resource that includes its value, which should be treated with the
  utmost security.
properties:
  data:
    $ref: ./ApiKeyWithValueData.yaml
